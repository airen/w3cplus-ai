---
const siteTitle = "W3Cplus";
const siteDescription = "AI 时代的前端实验室";
const slogan = "用 AI 构建前端产品，只记录自己试过的";
const year = new Date().getFullYear();
---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={siteDescription} />
    <title>{siteTitle} — {siteDescription}</title>
  </head>
  <body>
    <main>
      <!-- Hero -->
      <section class="hero">
        <p class="badge">2026 · 重新出发</p>
        <h1>W3Cplus</h1>
        <p class="tagline">{siteDescription}</p>
        <p class="slogan">{slogan}</p>
        <div class="hero-actions">
          <a href="#about" class="btn btn-primary">关于</a>
          <a href="#contact" class="btn btn-ghost">联系</a>
        </div>
      </section>

      <!-- Coming Soon -->
      <section id="about" class="section">
        <div class="section-header">
          <h2>Coming Soon</h2>
        </div>
        <div class="about-content">
          <p>
            W3Cplus 正在改版中，即将以全新面貌回归。
          </p>
          <p>
            2026 年，AI 正在重写前端的规则。我们带着一个新的问题回来：
            <strong>AI 能帮前端开发者做什么，哪些东西 AI 替代不了？</strong>
          </p>
          <p>
            这里会有 AI 前端工具的实测对比、实战教程、以及从 prompt 到上线的完整过程记录。
            工具站也在开发中，目标是让前端开发者用 AI 提效，而不是被 AI 取代。
          </p>
        </div>
      </section>

      <!-- Contact -->
      <section id="contact" class="section">
        <div class="section-header">
          <h2>联系</h2>
        </div>
        <p class="contact-text">
          想聊合作、提建议、或反馈问题，发邮件到
          <a href="mailto:airenliao@gmail.com">airenliao@gmail.com</a>
        </p>
      </section>

      <footer>
        <p>&copy; {year} {siteTitle}. Powered by Astro, deployed on Cloudflare.</p>
      </footer>
    </main>

    <style>
      :root {
        --bg: #0a0a0a;
        --fg: #f0f0f0;
        --muted: #777;
        --muted-2: #aaa;
        --accent: #00ff88;
        --accent-dim: #00cc6e;
        --card-bg: #141414;
        --card-hover: #1a1a1a;
        --border: #222;
        --border-hover: #333;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        background: var(--bg);
        color: var(--fg);
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
          "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
        line-height: 1.7;
        -webkit-font-smoothing: antialiased;
      }

      main {
        max-width: 720px;
        margin: 0 auto;
        padding: 0 24px;
      }

      /* Hero */
      .hero {
        padding: 100px 0 60px;
      }

      .badge {
        display: inline-block;
        font-size: 0.8rem;
        color: var(--accent);
        border: 1px solid var(--accent);
        padding: 4px 12px;
        border-radius: 20px;
        margin-bottom: 28px;
        letter-spacing: 0.05em;
      }

      .hero h1 {
        font-size: 3.2rem;
        font-weight: 800;
        letter-spacing: -0.03em;
        margin-bottom: 12px;
        background: linear-gradient(135deg, #f0f0f0 0%, #00ff88 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .tagline {
        font-size: 1.4rem;
        color: var(--fg);
        margin-bottom: 8px;
        font-weight: 500;
      }

      .slogan {
        font-size: 0.95rem;
        color: var(--muted);
        margin-bottom: 32px;
      }

      .hero-actions {
        display: flex;
        gap: 12px;
      }

      .btn {
        display: inline-block;
        padding: 10px 24px;
        border-radius: 8px;
        font-size: 0.9rem;
        text-decoration: none;
        transition: all 0.2s;
      }

      .btn-primary {
        background: var(--accent);
        color: #000;
        font-weight: 600;
      }

      .btn-primary:hover {
        background: var(--accent-dim);
      }

      .btn-ghost {
        border: 1px solid var(--border);
        color: var(--muted-2);
      }

      .btn-ghost:hover {
        border-color: var(--accent);
        color: var(--accent);
      }

      /* Sections */
      .section {
        padding: 48px 0;
        border-top: 1px solid var(--border);
      }

      .section-header {
        margin-bottom: 28px;
      }

      .section-header h2 {
        font-size: 1.3rem;
        color: var(--accent);
        margin-bottom: 6px;
      }

      /* About */
      .about-content p {
        color: var(--muted-2);
        margin-bottom: 16px;
      }

      .about-content strong {
        color: var(--fg);
      }

      /* Contact */
      .contact-text {
        color: var(--muted-2);
      }

      .contact-text a {
        color: var(--accent);
        text-decoration: none;
        border-bottom: 1px solid transparent;
        transition: border-color 0.2s;
      }

      .contact-text a:hover {
        border-bottom-color: var(--accent);
      }

      /* Footer */
      footer {
        padding: 40px 0 50px;
        border-top: 1px solid var(--border);
        text-align: center;
      }

      footer p {
        font-size: 0.8rem;
        color: var(--muted);
      }

      /* Responsive */
      @media (max-width: 640px) {
        .hero { padding: 70px 0 40px; }
        .hero h1 { font-size: 2.4rem; }
        .tagline { font-size: 1.15rem; }
      }
    </style>
  </body>
</html>
